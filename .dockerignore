.git
.github
.vscode
.idx
node_modules
npm-debug.log
*.env
# Add build artifacts if they are not needed in the final image directly
# e.g., if using multi-stage builds and intermediate stages produce them
# web-build # We need this for the serve command in the final stage
# functions/node_modules # If functions are deployed separately
